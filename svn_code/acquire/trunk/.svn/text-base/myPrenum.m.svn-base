function prenum = myPrenum(Number)
%  Generate recording number prefix for filenames

numstr = num2str(Number);
if length(numstr) == 1
    prenum = ['00' numstr];
elseif length(numstr) == 2
   prenum = ['0' numstr];
else
    prenum = numstr;
end
